# App Motoristas — MVP Fase 1 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir o MVP funcional do app de controle financeiro para motoristas — turno, combustível, despesas, dashboard com lucro líquido por hora e por km.

**Architecture:** Expo Router v3 (iOS + Android + Web PWA) consumindo Supabase (Postgres + Auth + Edge Functions). Cálculos de lucro real rodam server-side na Edge Function `calculate-shift`. i18n desde o dia 1 com i18next.

**Tech Stack:** Expo SDK 52+ · Expo Router v3 · TypeScript · Supabase JS v2 · i18next · expo-localization · React Native Reanimated · Victory Native (gráficos) · Jest + React Native Testing Library · Supabase CLI

## Global Constraints

- Node ≥ 20, npm ≥ 10
- Expo SDK 52+ (não usar SDK < 52)
- TypeScript strict mode habilitado
- RLS ativo em todas as tabelas — nenhuma tabela sem política
- Todos os valores monetários em centavos (integer) no banco; converter para exibição
- Distância em metros (integer); volume em mililitros (integer)
- Timestamps sempre em UTC no banco; converter para timezone do usuário na exibição
- Nenhum cálculo de lucro no cliente — tudo via Edge Function `calculate-shift`
- i18n obrigatório em todas as strings visíveis ao usuário (sem strings hardcoded em PT)

---

## File Structure

```
app-motorista/
├── app/
│   ├── _layout.tsx                 # Root layout, auth guard, i18n init
│   ├── (auth)/
│   │   ├── _layout.tsx
│   │   ├── login.tsx
│   │   └── register.tsx
│   ├── onboarding/
│   │   ├── _layout.tsx
│   │   ├── locale.tsx              # Idioma + país + moeda + unidade
│   │   ├── vehicle.tsx             # Cadastro do veículo
│   │   └── goal.tsx                # Meta mensal (opcional)
│   └── (tabs)/
│       ├── _layout.tsx
│       ├── index.tsx               # Dashboard
│       ├── shifts.tsx              # Lista + iniciar/encerrar turno
│       ├── fuel.tsx                # Combustível
│       ├── expenses.tsx            # Despesas
│       └── more.tsx                # Configurações
├── src/
│   ├── lib/
│   │   ├── supabase.ts             # Supabase client singleton
│   │   └── i18n.ts                 # i18next config
│   ├── types/
│   │   └── index.ts                # Todos os tipos TypeScript
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useProfile.ts
│   │   └── useActiveShift.ts
│   ├── services/
│   │   ├── profile.ts
│   │   ├── vehicles.ts
│   │   ├── shifts.ts
│   │   ├── fuel.ts
│   │   └── expenses.ts
│   └── utils/
│       ├── currency.ts             # Intl.NumberFormat helpers
│       └── units.ts                # km/mi, L/gal conversions
├── supabase/
│   ├── migrations/
│   │   └── 20260625000000_initial.sql
│   ├── functions/
│   │   └── calculate-shift/
│   │       ├── index.ts
│   │       └── index.test.ts
│   └── seed.sql
├── locales/
│   ├── pt.json
│   ├── en.json
│   └── es.json
└── __tests__/
    ├── utils/
    │   ├── currency.test.ts
    │   └── units.test.ts
    └── services/
        └── shifts.test.ts
```

---

### Task 1: Scaffold do Projeto

**Files:**
- Create: `app-motorista/` (raiz do projeto)
- Create: `app-motorista/app.json`
- Create: `app-motorista/.env.local`

- [ ] **1.1 Criar projeto Expo com Expo Router**

```bash
npx create-expo-app@latest app-motorista --template tabs
cd app-motorista
```

- [ ] **1.2 Instalar dependências**

```bash
npx expo install expo-localization @supabase/supabase-js react-native-url-polyfill \
  @react-native-async-storage/async-storage expo-secure-store \
  i18next react-i18next \
  react-native-reanimated react-native-gesture-handler \
  victory-native react-native-svg \
  expo-router

npm install --save-dev @testing-library/react-native @testing-library/jest-native \
  jest jest-expo ts-jest
```

- [ ] **1.3 Configurar jest.config.js**

```js
// jest.config.js
module.exports = {
  preset: 'jest-expo',
  setupFilesAfterFramework: ['@testing-library/jest-native/extend-expect'],
  transformIgnorePatterns: [
    'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg)',
  ],
};
```

- [ ] **1.4 Criar .env.local**

```bash
# .env.local  (nunca commitar)
EXPO_PUBLIC_SUPABASE_URL=https://SEU_PROJETO.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sua_anon_key_aqui
```

- [ ] **1.5 Commit**

```bash
git add -A
git commit -m "chore: scaffold expo router v3 + dependências"
```

---

### Task 2: Supabase — Schema + RLS + Seed

**Files:**
- Create: `supabase/migrations/20260625000000_initial.sql`
- Create: `supabase/seed.sql`

- [ ] **2.1 Instalar Supabase CLI e inicializar**

```bash
npm install -g supabase
supabase init
supabase start   # inicia instância local
```

- [ ] **2.2 Escrever migration**

```sql
-- supabase/migrations/20260625000000_initial.sql

-- EXTENSIONS
create extension if not exists "uuid-ossp";

-- PROFILES
create table profiles (
  id uuid primary key references auth.users on delete cascade,
  name text not null,
  country text not null default 'BR',
  city text,
  currency_code text not null default 'BRL',
  distance_unit text not null default 'km' check (distance_unit in ('km', 'mi')),
  volume_unit text not null default 'liters' check (volume_unit in ('liters', 'gallons')),
  timezone text not null default 'America/Sao_Paulo',
  locale text not null default 'pt-BR',
  onboarding_done boolean not null default false,
  created_at timestamptz not null default now()
);
alter table profiles enable row level security;
create policy "own profile" on profiles for all using (auth.uid() = id);

-- VEHICLES
create table vehicles (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid not null references profiles on delete cascade,
  name text not null,
  brand text not null,
  model text not null,
  year smallint not null,
  plate text,
  fuel_type text not null check (fuel_type in ('gasoline','ethanol','diesel','gnv','electric','hybrid')),
  avg_consumption_per_100 integer not null, -- ml per 100 distance_unit
  ownership_type text not null check (ownership_type in ('own','rent','financed')),
  monthly_cost_cents integer not null default 0,
  monthly_insurance_cents integer not null default 0,
  current_odometer integer not null default 0, -- in meters
  purchase_price_cents integer,
  purchase_date date,
  target_swap_years smallint,
  target_swap_budget_cents integer,
  is_taxi boolean not null default false,
  taxi_license_monthly_cents integer not null default 0,
  created_at timestamptz not null default now()
);
alter table vehicles enable row level security;
create policy "own vehicles" on vehicles for all using (auth.uid() = user_id);

-- PLATFORMS (sem RLS — leitura pública)
create table platforms (
  id uuid primary key default uuid_generate_v4(),
  name text not null,
  country_code text,   -- null = global
  type text not null check (type in ('rideshare','taxi_app','taxi_conventional','delivery')),
  active boolean not null default true
);

-- SHIFTS
create table shifts (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid not null references profiles on delete cascade,
  vehicle_id uuid not null references vehicles on delete cascade,
  started_at timestamptz not null,
  ended_at timestamptz,
  start_odometer integer not null,  -- meters
  end_odometer integer,             -- meters
  tips_cents integer not null default 0,
  bonuses_cents integer not null default 0,
  tolls_cents integer not null default 0,
  parking_cents integer not null default 0,
  food_cents integer not null default 0,
  region text,
  notes text,
  -- cached calculation result (from edge function)
  calc jsonb,
  created_at timestamptz not null default now()
);
alter table shifts enable row level security;
create policy "own shifts" on shifts for all using (auth.uid() = user_id);

-- SHIFT EARNINGS PER PLATFORM
create table shift_earnings (
  id uuid primary key default uuid_generate_v4(),
  shift_id uuid not null references shifts on delete cascade,
  platform_id uuid not null references platforms,
  gross_amount_cents integer not null
);
alter table shift_earnings enable row level security;
create policy "own shift earnings" on shift_earnings for all
  using (auth.uid() = (select user_id from shifts where id = shift_id));

-- FUEL ENTRIES
create table fuel_entries (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid not null references profiles on delete cascade,
  vehicle_id uuid not null references vehicles on delete cascade,
  filled_at timestamptz not null,
  odometer integer not null,  -- meters
  fuel_type text not null,
  volume_ml integer not null,
  total_amount_cents integer not null,
  price_per_unit_cents integer not null, -- per liter/gallon in cents
  station_name text,
  is_full_tank boolean not null default true,
  notes text,
  created_at timestamptz not null default now()
);
alter table fuel_entries enable row level security;
create policy "own fuel" on fuel_entries for all using (auth.uid() = user_id);

-- EXPENSE CATEGORIES
create table expense_categories (
  id uuid primary key default uuid_generate_v4(),
  name_key text not null,  -- i18n key
  type text not null check (type in ('fixed','variable')),
  is_system boolean not null default false
);

-- EXPENSES
create table expenses (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid not null references profiles on delete cascade,
  vehicle_id uuid references vehicles on delete set null,
  category_id uuid not null references expense_categories,
  expense_date date not null,
  amount_cents integer not null,
  description text,
  is_recurring boolean not null default false,
  recurrence_period text check (recurrence_period in ('daily','weekly','monthly','yearly')),
  created_at timestamptz not null default now()
);
alter table expenses enable row level security;
create policy "own expenses" on expenses for all using (auth.uid() = user_id);

-- GOALS
create table goals (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid not null references profiles on delete cascade,
  type text not null check (type in ('daily','weekly','monthly','per_hour','per_km')),
  target_amount_cents integer not null,
  starts_at date not null,
  ends_at date,
  created_at timestamptz not null default now()
);
alter table goals enable row level security;
create policy "own goals" on goals for all using (auth.uid() = user_id);
```

- [ ] **2.3 Escrever seed.sql**

```sql
-- supabase/seed.sql

insert into platforms (name, country_code, type) values
  ('Uber', null, 'rideshare'),
  ('99', 'BR', 'rideshare'),
  ('inDrive', null, 'rideshare'),
  ('Lyft', 'US', 'rideshare'),
  ('Bolt', null, 'rideshare'),
  ('Cabify', null, 'rideshare'),
  ('DiDi', null, 'rideshare'),
  ('Grab', null, 'rideshare'),
  ('FreeNow', null, 'rideshare'),
  ('99Táxi', 'BR', 'taxi_app'),
  ('Easy Taxi', 'BR', 'taxi_app'),
  ('inDriver Táxi', null, 'taxi_app'),
  ('Convencional / Rádio Táxi', 'BR', 'taxi_conventional'),
  ('Conventional / Street', 'US', 'taxi_conventional');

insert into expense_categories (name_key, type, is_system) values
  ('expense.rent', 'fixed', true),
  ('expense.financing', 'fixed', true),
  ('expense.insurance', 'fixed', true),
  ('expense.internet', 'fixed', true),
  ('expense.tracker', 'fixed', true),
  ('expense.licensing', 'fixed', true),
  ('expense.taxi_license', 'fixed', true),
  ('expense.fuel', 'variable', true),
  ('expense.car_wash', 'variable', true),
  ('expense.maintenance', 'variable', true),
  ('expense.tires', 'variable', true),
  ('expense.oil_change', 'variable', true),
  ('expense.tolls', 'variable', true),
  ('expense.parking', 'variable', true),
  ('expense.food', 'variable', true),
  ('expense.other', 'variable', true);
```

- [ ] **2.4 Aplicar migration e seed localmente**

```bash
supabase db reset
# Output esperado: "Finished supabase db reset"
```

- [ ] **2.5 Commit**

```bash
git add supabase/
git commit -m "feat: schema inicial supabase + RLS + seed de plataformas"
```

---

### Task 3: Tipos TypeScript + Supabase Client + i18n

**Files:**
- Create: `src/types/index.ts`
- Create: `src/lib/supabase.ts`
- Create: `src/lib/i18n.ts`
- Create: `src/utils/currency.ts`
- Create: `src/utils/units.ts`
- Create: `locales/pt.json`, `locales/en.json`, `locales/es.json`

- [ ] **3.1 Escrever testes de utils**

```ts
// __tests__/utils/currency.test.ts
import { formatMoney, centsToDecimal, decimalToCents } from '../../src/utils/currency';

test('formatMoney BRL', () => {
  expect(formatMoney(32000, 'BRL', 'pt-BR')).toBe('R$ 320,00');
});
test('formatMoney USD', () => {
  expect(formatMoney(100, 'USD', 'en-US')).toBe('$1.00');
});
test('centsToDecimal', () => {
  expect(centsToDecimal(32000)).toBe(320.0);
});
test('decimalToCents', () => {
  expect(decimalToCents(320.0)).toBe(32000);
});
```

```ts
// __tests__/utils/units.test.ts
import { metersToDisplay, mlToDisplay, displayToMeters, displayToMl } from '../../src/utils/units';

test('km display', () => {
  expect(metersToDisplay(176000, 'km')).toBeCloseTo(176, 1);
});
test('mi display', () => {
  expect(metersToDisplay(1609, 'mi')).toBeCloseTo(1, 1);
});
test('liters display', () => {
  expect(mlToDisplay(45000, 'liters')).toBeCloseTo(45, 1);
});
test('displayToMeters km', () => {
  expect(displayToMeters(176, 'km')).toBe(176000);
});
```

- [ ] **3.2 Rodar testes (devem falhar)**

```bash
npx jest __tests__/utils/
# Expected: FAIL — currency/units not defined
```

- [ ] **3.3 Implementar utils**

```ts
// src/utils/currency.ts
export const centsToDecimal = (cents: number): number => cents / 100;
export const decimalToCents = (value: number): number => Math.round(value * 100);

export const formatMoney = (
  cents: number,
  currencyCode: string,
  locale: string
): string =>
  new Intl.NumberFormat(locale, {
    style: 'currency',
    currency: currencyCode,
  }).format(centsToDecimal(cents));
```

```ts
// src/utils/units.ts
const MI_PER_METER = 0.000621371;
const GAL_PER_ML = 0.000264172;

export const metersToDisplay = (meters: number, unit: 'km' | 'mi'): number =>
  unit === 'km' ? meters / 1000 : meters * MI_PER_METER;

export const displayToMeters = (value: number, unit: 'km' | 'mi'): number =>
  unit === 'km' ? Math.round(value * 1000) : Math.round(value / MI_PER_METER);

export const mlToDisplay = (ml: number, unit: 'liters' | 'gallons'): number =>
  unit === 'liters' ? ml / 1000 : ml * GAL_PER_ML;

export const displayToMl = (value: number, unit: 'liters' | 'gallons'): number =>
  unit === 'liters' ? Math.round(value * 1000) : Math.round(value / GAL_PER_ML);
```

- [ ] **3.4 Rodar testes (devem passar)**

```bash
npx jest __tests__/utils/
# Expected: PASS (4 tests)
```

- [ ] **3.5 Criar tipos TypeScript**

```ts
// src/types/index.ts
export type DistanceUnit = 'km' | 'mi';
export type VolumeUnit = 'liters' | 'gallons';
export type FuelType = 'gasoline' | 'ethanol' | 'diesel' | 'gnv' | 'electric' | 'hybrid';
export type OwnershipType = 'own' | 'rent' | 'financed';
export type PlatformType = 'rideshare' | 'taxi_app' | 'taxi_conventional' | 'delivery';

export interface Profile {
  id: string;
  name: string;
  country: string;
  city?: string;
  currency_code: string;
  distance_unit: DistanceUnit;
  volume_unit: VolumeUnit;
  timezone: string;
  locale: string;
  onboarding_done: boolean;
  created_at: string;
}

export interface Vehicle {
  id: string;
  user_id: string;
  name: string;
  brand: string;
  model: string;
  year: number;
  plate?: string;
  fuel_type: FuelType;
  avg_consumption_per_100: number;
  ownership_type: OwnershipType;
  monthly_cost_cents: number;
  monthly_insurance_cents: number;
  current_odometer: number;
  purchase_price_cents?: number;
  purchase_date?: string;
  target_swap_years?: number;
  target_swap_budget_cents?: number;
  is_taxi: boolean;
  taxi_license_monthly_cents: number;
  created_at: string;
}

export interface Platform {
  id: string;
  name: string;
  country_code?: string;
  type: PlatformType;
  active: boolean;
}

export interface ShiftEarning {
  id: string;
  shift_id: string;
  platform_id: string;
  gross_amount_cents: number;
  platform?: Platform;
}

export interface ShiftCalc {
  duration_hours: number;
  distance_meters: number;
  gross_cents: number;
  fuel_cost_cents: number;
  allocated_fixed_cents: number;
  net_cents: number;
  net_per_hour_cents: number;
  net_per_meter_cents: number;
}

export interface Shift {
  id: string;
  user_id: string;
  vehicle_id: string;
  started_at: string;
  ended_at?: string;
  start_odometer: number;
  end_odometer?: number;
  tips_cents: number;
  bonuses_cents: number;
  tolls_cents: number;
  parking_cents: number;
  food_cents: number;
  region?: string;
  notes?: string;
  calc?: ShiftCalc;
  created_at: string;
  shift_earnings?: ShiftEarning[];
}

export interface FuelEntry {
  id: string;
  user_id: string;
  vehicle_id: string;
  filled_at: string;
  odometer: number;
  fuel_type: FuelType;
  volume_ml: number;
  total_amount_cents: number;
  price_per_unit_cents: number;
  station_name?: string;
  is_full_tank: boolean;
  notes?: string;
}

export interface ExpenseCategory {
  id: string;
  name_key: string;
  type: 'fixed' | 'variable';
  is_system: boolean;
}

export interface Expense {
  id: string;
  user_id: string;
  vehicle_id?: string;
  category_id: string;
  expense_date: string;
  amount_cents: number;
  description?: string;
  is_recurring: boolean;
  recurrence_period?: 'daily' | 'weekly' | 'monthly' | 'yearly';
  category?: ExpenseCategory;
}

export interface Goal {
  id: string;
  user_id: string;
  type: 'daily' | 'weekly' | 'monthly' | 'per_hour' | 'per_km';
  target_amount_cents: number;
  starts_at: string;
  ends_at?: string;
}
```

- [ ] **3.6 Criar Supabase client**

```ts
// src/lib/supabase.ts
import 'react-native-url-polyfill/auto';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: AsyncStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});
```

- [ ] **3.7 Criar arquivos de localização (strings MVP)**

```json
// locales/pt.json
{
  "app_name": "Meu Lucro",
  "auth": {
    "login": "Entrar",
    "register": "Criar conta",
    "email": "E-mail",
    "password": "Senha",
    "name": "Nome completo",
    "logout": "Sair"
  },
  "onboarding": {
    "language_title": "Configure seu perfil",
    "country": "País",
    "currency": "Moeda",
    "distance_unit": "Distância",
    "volume_unit": "Volume",
    "vehicle_title": "Seu veículo",
    "brand": "Marca",
    "model": "Modelo",
    "year": "Ano",
    "fuel_type": "Combustível",
    "ownership": "Tipo de uso",
    "monthly_cost": "Custo mensal (aluguel/parcela)",
    "insurance": "Seguro mensal",
    "is_taxi": "Sou taxista",
    "taxi_license": "Custo do alvará/mês",
    "goal_title": "Meta mensal (opcional)",
    "goal_placeholder": "Ex: 5000",
    "skip": "Pular",
    "next": "Próximo",
    "finish": "Começar"
  },
  "tabs": {
    "dashboard": "Início",
    "shifts": "Turnos",
    "fuel": "Combustível",
    "expenses": "Despesas",
    "more": "Mais"
  },
  "dashboard": {
    "today": "Hoje",
    "gross": "Bruto",
    "net": "Líquido",
    "hours": "horas",
    "km": "km",
    "net_per_hour": "Líquido/hora",
    "net_per_km": "Líquido/km",
    "best_platform": "Melhor app",
    "goal_progress": "Meta do dia",
    "active_shift": "Turno em andamento",
    "no_shifts": "Nenhum turno hoje"
  },
  "shift": {
    "start": "Iniciar turno",
    "end": "Encerrar turno",
    "odometer_start": "Km inicial",
    "odometer_end": "Km final",
    "earnings": "Ganhos por plataforma",
    "add_platform": "Adicionar plataforma",
    "costs": "Custos do turno",
    "tolls": "Pedágios",
    "parking": "Estacionamento",
    "food": "Alimentação / snacks",
    "tips": "Gorjetas",
    "bonuses": "Bônus",
    "save": "Salvar turno",
    "result_net": "Lucro líquido",
    "result_hours": "Horas trabalhadas",
    "result_km": "Km rodados"
  },
  "fuel": {
    "title": "Abastecimentos",
    "add": "Registrar abastecimento",
    "odometer": "Km atual",
    "liters": "Litros",
    "total": "Valor total",
    "price_per_liter": "Preço por litro",
    "full_tank": "Tanque cheio",
    "station": "Posto (opcional)",
    "save": "Salvar"
  },
  "expense": {
    "title": "Despesas",
    "add": "Nova despesa",
    "category": "Categoria",
    "amount": "Valor",
    "date": "Data",
    "description": "Descrição (opcional)",
    "recurring": "Despesa recorrente",
    "save": "Salvar"
  },
  "expense_category": {
    "expense.rent": "Aluguel do carro",
    "expense.financing": "Financiamento",
    "expense.insurance": "Seguro",
    "expense.internet": "Internet / Celular",
    "expense.tracker": "Rastreador",
    "expense.licensing": "Licenciamento / IPVA",
    "expense.taxi_license": "Alvará de táxi",
    "expense.fuel": "Combustível",
    "expense.car_wash": "Lavagem",
    "expense.maintenance": "Manutenção",
    "expense.tires": "Pneus",
    "expense.oil_change": "Troca de óleo",
    "expense.tolls": "Pedágios",
    "expense.parking": "Estacionamento",
    "expense.food": "Alimentação",
    "expense.other": "Outros"
  },
  "common": {
    "save": "Salvar",
    "cancel": "Cancelar",
    "delete": "Excluir",
    "edit": "Editar",
    "loading": "Carregando...",
    "error": "Algo deu errado",
    "required": "Campo obrigatório"
  }
}
```

```json
// locales/en.json
{
  "app_name": "My Earnings",
  "auth": { "login": "Sign in", "register": "Create account", "email": "Email", "password": "Password", "name": "Full name", "logout": "Sign out" },
  "onboarding": { "language_title": "Set up your profile", "country": "Country", "currency": "Currency", "distance_unit": "Distance", "volume_unit": "Volume", "vehicle_title": "Your vehicle", "brand": "Brand", "model": "Model", "year": "Year", "fuel_type": "Fuel type", "ownership": "Vehicle type", "monthly_cost": "Monthly cost (rent/payment)", "insurance": "Monthly insurance", "is_taxi": "I'm a taxi driver", "taxi_license": "License fee/month", "goal_title": "Monthly goal (optional)", "goal_placeholder": "e.g. 2000", "skip": "Skip", "next": "Next", "finish": "Get started" },
  "tabs": { "dashboard": "Home", "shifts": "Shifts", "fuel": "Fuel", "expenses": "Expenses", "more": "More" },
  "dashboard": { "today": "Today", "gross": "Gross", "net": "Net", "hours": "hours", "km": "miles", "net_per_hour": "Net/hour", "net_per_km": "Net/mile", "best_platform": "Best app", "goal_progress": "Daily goal", "active_shift": "Shift in progress", "no_shifts": "No shifts today" },
  "shift": { "start": "Start shift", "end": "End shift", "odometer_start": "Starting odometer", "odometer_end": "Ending odometer", "earnings": "Earnings per platform", "add_platform": "Add platform", "costs": "Shift costs", "tolls": "Tolls", "parking": "Parking", "food": "Food / snacks", "tips": "Tips", "bonuses": "Bonuses", "save": "Save shift", "result_net": "Net earnings", "result_hours": "Hours worked", "result_km": "Miles driven" },
  "fuel": { "title": "Fuel log", "add": "Log fill-up", "odometer": "Current odometer", "liters": "Gallons", "total": "Total amount", "price_per_liter": "Price per gallon", "full_tank": "Full tank", "station": "Station (optional)", "save": "Save" },
  "expense": { "title": "Expenses", "add": "New expense", "category": "Category", "amount": "Amount", "date": "Date", "description": "Description (optional)", "recurring": "Recurring expense", "save": "Save" },
  "expense_category": { "expense.rent": "Car rental", "expense.financing": "Financing payment", "expense.insurance": "Insurance", "expense.internet": "Internet / Phone", "expense.tracker": "GPS tracker", "expense.licensing": "Registration / Tax", "expense.taxi_license": "Taxi license", "expense.fuel": "Fuel", "expense.car_wash": "Car wash", "expense.maintenance": "Maintenance", "expense.tires": "Tires", "expense.oil_change": "Oil change", "expense.tolls": "Tolls", "expense.parking": "Parking", "expense.food": "Food", "expense.other": "Other" },
  "common": { "save": "Save", "cancel": "Cancel", "delete": "Delete", "edit": "Edit", "loading": "Loading...", "error": "Something went wrong", "required": "Required field" }
}
```

```json
// locales/es.json
{
  "app_name": "Mis Ganancias",
  "auth": { "login": "Iniciar sesión", "register": "Crear cuenta", "email": "Correo", "password": "Contraseña", "name": "Nombre completo", "logout": "Cerrar sesión" },
  "onboarding": { "language_title": "Configura tu perfil", "country": "País", "currency": "Moneda", "distance_unit": "Distancia", "volume_unit": "Volumen", "vehicle_title": "Tu vehículo", "brand": "Marca", "model": "Modelo", "year": "Año", "fuel_type": "Combustible", "ownership": "Tipo de uso", "monthly_cost": "Costo mensual (alquiler/cuota)", "insurance": "Seguro mensual", "is_taxi": "Soy taxista", "taxi_license": "Licencia/mes", "goal_title": "Meta mensual (opcional)", "goal_placeholder": "Ej: 1500", "skip": "Omitir", "next": "Siguiente", "finish": "Empezar" },
  "tabs": { "dashboard": "Inicio", "shifts": "Turnos", "fuel": "Combustible", "expenses": "Gastos", "more": "Más" },
  "dashboard": { "today": "Hoy", "gross": "Bruto", "net": "Neto", "hours": "horas", "km": "km", "net_per_hour": "Neto/hora", "net_per_km": "Neto/km", "best_platform": "Mejor app", "goal_progress": "Meta del día", "active_shift": "Turno en curso", "no_shifts": "Sin turnos hoy" },
  "shift": { "start": "Iniciar turno", "end": "Finalizar turno", "odometer_start": "Km inicial", "odometer_end": "Km final", "earnings": "Ganancias por plataforma", "add_platform": "Agregar plataforma", "costs": "Costos del turno", "tolls": "Peajes", "parking": "Estacionamiento", "food": "Comida / snacks", "tips": "Propinas", "bonuses": "Bonos", "save": "Guardar turno", "result_net": "Ganancia neta", "result_hours": "Horas trabajadas", "result_km": "Km recorridos" },
  "fuel": { "title": "Combustible", "add": "Registrar carga", "odometer": "Km actual", "liters": "Litros", "total": "Monto total", "price_per_liter": "Precio por litro", "full_tank": "Tanque lleno", "station": "Estación (opcional)", "save": "Guardar" },
  "expense": { "title": "Gastos", "add": "Nuevo gasto", "category": "Categoría", "amount": "Monto", "date": "Fecha", "description": "Descripción (opcional)", "recurring": "Gasto recurrente", "save": "Guardar" },
  "expense_category": { "expense.rent": "Alquiler del auto", "expense.financing": "Financiamiento", "expense.insurance": "Seguro", "expense.internet": "Internet / Celular", "expense.tracker": "Rastreador", "expense.licensing": "Patente / Impuesto", "expense.taxi_license": "Licencia de taxi", "expense.fuel": "Combustible", "expense.car_wash": "Lavado", "expense.maintenance": "Mantenimiento", "expense.tires": "Neumáticos", "expense.oil_change": "Cambio de aceite", "expense.tolls": "Peajes", "expense.parking": "Estacionamiento", "expense.food": "Comida", "expense.other": "Otros" },
  "common": { "save": "Guardar", "cancel": "Cancelar", "delete": "Eliminar", "edit": "Editar", "loading": "Cargando...", "error": "Algo salió mal", "required": "Campo requerido" }
}
```

- [ ] **3.8 Configurar i18n**

```ts
// src/lib/i18n.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import * as Localization from 'expo-localization';
import pt from '../../locales/pt.json';
import en from '../../locales/en.json';
import es from '../../locales/es.json';

i18n.use(initReactI18next).init({
  resources: { pt: { translation: pt }, en: { translation: en }, es: { translation: es } },
  lng: Localization.getLocales()[0]?.languageCode ?? 'pt',
  fallbackLng: 'pt',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **3.9 Commit**

```bash
git add src/ locales/ __tests__/utils/
git commit -m "feat: tipos, supabase client, i18n, utils de moeda e unidade"
```

---

### Task 4: Edge Function — calculate-shift

**Files:**
- Create: `supabase/functions/calculate-shift/index.ts`
- Create: `supabase/functions/calculate-shift/index.test.ts`

- [ ] **4.1 Escrever testes (Deno)**

```ts
// supabase/functions/calculate-shift/index.test.ts
import { assertEquals, assertAlmostEquals } from 'https://deno.land/std/assert/mod.ts';
import { calculateShift, type CalcInput } from './index.ts';

const base: CalcInput = {
  started_at: '2026-06-25T08:00:00Z',
  ended_at: '2026-06-25T16:10:00Z',
  start_odometer: 87000000,   // 87.000 km em metros
  end_odometer: 87176000,     // +176 km
  gross_cents: 32000,         // R$ 320,00
  tips_cents: 0,
  bonuses_cents: 0,
  tolls_cents: 0,
  parking_cents: 0,
  food_cents: 0,
  avg_consumption_per_100: 10000, // 10L/100km em ml
  last_fuel_price_per_unit_cents: 599, // R$ 5,99/L
  monthly_fixed_cents: 120000, // R$ 1.200/mês
  days_in_month: 30,
  shift_days: 1,
};

Deno.test('duration_hours', () => {
  const r = calculateShift(base);
  assertAlmostEquals(r.duration_hours, 8.167, 0.01);
});

Deno.test('distance_meters', () => {
  const r = calculateShift(base);
  assertEquals(r.distance_meters, 176000);
});

Deno.test('fuel_cost_cents', () => {
  // 176km * 10L/100km = 17.6L * R$5.99 = R$105.42 ~ 10542 cents
  const r = calculateShift(base);
  assertAlmostEquals(r.fuel_cost_cents, 10542, 50);
});

Deno.test('net_cents', () => {
  const r = calculateShift(base);
  // gross 32000 - fuel ~10542 - fixed_alloc 4000 = ~17458
  assertAlmostEquals(r.net_cents, 17458, 100);
});

Deno.test('net_per_hour_cents', () => {
  const r = calculateShift(base);
  assertAlmostEquals(r.net_per_hour_cents, r.net_cents / r.duration_hours, 10);
});
```

- [ ] **4.2 Rodar testes (devem falhar)**

```bash
deno test supabase/functions/calculate-shift/index.test.ts
# Expected: error — calculateShift not found
```

- [ ] **4.3 Implementar Edge Function**

```ts
// supabase/functions/calculate-shift/index.ts
import { serve } from 'https://deno.land/std/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

export interface CalcInput {
  started_at: string;
  ended_at: string;
  start_odometer: number;    // meters
  end_odometer: number;      // meters
  gross_cents: number;
  tips_cents: number;
  bonuses_cents: number;
  tolls_cents: number;
  parking_cents: number;
  food_cents: number;
  avg_consumption_per_100: number;     // ml per 100 distance_unit
  last_fuel_price_per_unit_cents: number;
  monthly_fixed_cents: number;
  days_in_month: number;
  shift_days: number;
}

export interface CalcResult {
  duration_hours: number;
  distance_meters: number;
  gross_cents: number;
  fuel_cost_cents: number;
  allocated_fixed_cents: number;
  net_cents: number;
  net_per_hour_cents: number;
  net_per_meter_cents: number;
}

export function calculateShift(input: CalcInput): CalcResult {
  const start = new Date(input.started_at).getTime();
  const end = new Date(input.ended_at).getTime();
  const duration_hours = (end - start) / 3_600_000;

  const distance_meters = input.end_odometer - input.start_odometer;
  const distance_per_100 = distance_meters / 1000 / 100; // in 100-km units
  const volume_ml = distance_per_100 * input.avg_consumption_per_100;
  const volume_units = volume_ml / 1000; // liters
  const fuel_cost_cents = Math.round(volume_units * input.last_fuel_price_per_unit_cents);

  const daily_fixed_cents = input.monthly_fixed_cents / input.days_in_month;
  const allocated_fixed_cents = Math.round(daily_fixed_cents * input.shift_days);

  const variable_costs = input.tolls_cents + input.parking_cents + input.food_cents;
  const gross_cents = input.gross_cents + input.tips_cents + input.bonuses_cents;
  const net_cents = gross_cents - fuel_cost_cents - allocated_fixed_cents - variable_costs;

  const net_per_hour_cents = duration_hours > 0 ? net_cents / duration_hours : 0;
  const net_per_meter_cents = distance_meters > 0 ? net_cents / distance_meters : 0;

  return {
    duration_hours,
    distance_meters,
    gross_cents,
    fuel_cost_cents,
    allocated_fixed_cents,
    net_cents,
    net_per_hour_cents,
    net_per_meter_cents,
  };
}

serve(async (req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  );

  const { shift_id } = await req.json();

  const { data: shift } = await supabase
    .from('shifts')
    .select('*, vehicles(*), shift_earnings(*)')
    .eq('id', shift_id)
    .single();

  if (!shift?.ended_at || !shift?.end_odometer) {
    return new Response(JSON.stringify({ error: 'shift not ended' }), { status: 400 });
  }

  // fetch last fuel price for vehicle
  const { data: lastFuel } = await supabase
    .from('fuel_entries')
    .select('price_per_unit_cents')
    .eq('vehicle_id', shift.vehicle_id)
    .order('filled_at', { ascending: false })
    .limit(1)
    .single();

  const gross_cents = shift.shift_earnings.reduce(
    (sum: number, e: { gross_amount_cents: number }) => sum + e.gross_amount_cents, 0
  );

  const monthly_fixed_cents =
    shift.vehicles.monthly_cost_cents +
    shift.vehicles.monthly_insurance_cents +
    shift.vehicles.taxi_license_monthly_cents;

  const input: CalcInput = {
    started_at: shift.started_at,
    ended_at: shift.ended_at,
    start_odometer: shift.start_odometer,
    end_odometer: shift.end_odometer,
    gross_cents,
    tips_cents: shift.tips_cents,
    bonuses_cents: shift.bonuses_cents,
    tolls_cents: shift.tolls_cents,
    parking_cents: shift.parking_cents,
    food_cents: shift.food_cents,
    avg_consumption_per_100: shift.vehicles.avg_consumption_per_100,
    last_fuel_price_per_unit_cents: lastFuel?.price_per_unit_cents ?? 0,
    monthly_fixed_cents,
    days_in_month: new Date(
      new Date(shift.started_at).getFullYear(),
      new Date(shift.started_at).getMonth() + 1, 0
    ).getDate(),
    shift_days: 1,
  };

  const calc = calculateShift(input);

  await supabase
    .from('shifts')
    .update({ calc })
    .eq('id', shift_id);

  return new Response(JSON.stringify(calc), {
    headers: { 'Content-Type': 'application/json' },
  });
});
```

- [ ] **4.4 Rodar testes (devem passar)**

```bash
deno test supabase/functions/calculate-shift/index.test.ts
# Expected: PASS (5 tests)
```

- [ ] **4.5 Deploy function localmente e testar via curl**

```bash
supabase functions serve calculate-shift
# em outro terminal:
curl -X POST http://localhost:54321/functions/v1/calculate-shift \
  -H "Content-Type: application/json" \
  -d '{"shift_id": "UUID_DE_UM_TURNO_TESTE"}'
# Expected: JSON com duration_hours, net_cents, etc.
```

- [ ] **4.6 Commit**

```bash
git add supabase/functions/
git commit -m "feat: edge function calculate-shift com testes"
```

---

### Task 5: Auth — Login e Register

**Files:**
- Create: `src/hooks/useAuth.ts`
- Create: `app/(auth)/_layout.tsx`
- Create: `app/(auth)/login.tsx`
- Create: `app/(auth)/register.tsx`

- [ ] **5.1 Implementar useAuth hook**

```ts
// src/hooks/useAuth.ts
import { useEffect, useState } from 'react';
import { Session } from '@supabase/supabase-js';
import { supabase } from '../lib/supabase';

export function useAuth() {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session);
      setLoading(false);
    });
    const { data: listener } = supabase.auth.onAuthStateChange((_event, s) => {
      setSession(s);
    });
    return () => listener.subscription.unsubscribe();
  }, []);

  const signIn = (email: string, password: string) =>
    supabase.auth.signInWithPassword({ email, password });

  const signUp = (email: string, password: string) =>
    supabase.auth.signUp({ email, password });

  const signOut = () => supabase.auth.signOut();

  return { session, loading, signIn, signUp, signOut };
}
```

- [ ] **5.2 Root layout com auth guard**

```tsx
// app/_layout.tsx
import '../../src/lib/i18n';
import { Slot, useRouter, useSegments } from 'expo-router';
import { useEffect } from 'react';
import { useAuth } from '../src/hooks/useAuth';

export default function RootLayout() {
  const { session, loading } = useAuth();
  const router = useRouter();
  const segments = useSegments();

  useEffect(() => {
    if (loading) return;
    const inAuth = segments[0] === '(auth)';
    if (!session && !inAuth) router.replace('/(auth)/login');
    if (session && inAuth) router.replace('/(tabs)');
  }, [session, loading]);

  return <Slot />;
}
```

- [ ] **5.3 Login screen**

```tsx
// app/(auth)/login.tsx
import { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet } from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { useAuth } from '../../src/hooks/useAuth';

export default function LoginScreen() {
  const { t } = useTranslation();
  const { signIn } = useAuth();
  const router = useRouter();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');

  const handleLogin = async () => {
    const { error: e } = await signIn(email, password);
    if (e) setError(e.message);
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('app_name')}</Text>
      {error ? <Text style={s.error}>{error}</Text> : null}
      <TextInput
        style={s.input}
        placeholder={t('auth.email')}
        value={email}
        onChangeText={setEmail}
        autoCapitalize="none"
        keyboardType="email-address"
      />
      <TextInput
        style={s.input}
        placeholder={t('auth.password')}
        value={password}
        onChangeText={setPassword}
        secureTextEntry
      />
      <Button title={t('auth.login')} onPress={handleLogin} />
      <Button
        title={t('auth.register')}
        onPress={() => router.push('/(auth)/register')}
      />
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24 },
  title: { fontSize: 28, fontWeight: 'bold', marginBottom: 32, textAlign: 'center' },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  error: { color: 'red', marginBottom: 12 },
});
```

- [ ] **5.4 Register screen**

```tsx
// app/(auth)/register.tsx
import { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet } from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { useAuth } from '../../src/hooks/useAuth';

export default function RegisterScreen() {
  const { t } = useTranslation();
  const { signUp } = useAuth();
  const router = useRouter();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');

  const handleRegister = async () => {
    const { error: e } = await signUp(email, password);
    if (e) { setError(e.message); return; }
    router.replace('/onboarding/locale');
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('auth.register')}</Text>
      {error ? <Text style={s.error}>{error}</Text> : null}
      <TextInput style={s.input} placeholder={t('auth.email')} value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" />
      <TextInput style={s.input} placeholder={t('auth.password')} value={password} onChangeText={setPassword} secureTextEntry />
      <Button title={t('auth.register')} onPress={handleRegister} />
      <Button title={t('auth.login')} onPress={() => router.back()} />
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24 },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 32, textAlign: 'center' },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 12 },
  error: { color: 'red', marginBottom: 12 },
});
```

- [ ] **5.5 Testar manualmente: login → tabs, novo usuário → onboarding**

```bash
npx expo start
# Abrir no simulador, criar conta, verificar redirecionamento para /onboarding/locale
```

- [ ] **5.6 Commit**

```bash
git add app/(auth)/ src/hooks/useAuth.ts
git commit -m "feat: auth — login e register com redirecionamento"
```

---

### Task 6: Onboarding

**Files:**
- Create: `src/services/profile.ts`
- Create: `src/services/vehicles.ts`
- Create: `src/hooks/useProfile.ts`
- Create: `app/onboarding/locale.tsx`
- Create: `app/onboarding/vehicle.tsx`
- Create: `app/onboarding/goal.tsx`

- [ ] **6.1 Services de profile e vehicle**

```ts
// src/services/profile.ts
import { supabase } from '../lib/supabase';
import type { Profile } from '../types';

export async function getProfile(userId: string): Promise<Profile | null> {
  const { data } = await supabase.from('profiles').select('*').eq('id', userId).single();
  return data;
}

export async function upsertProfile(profile: Partial<Profile> & { id: string }): Promise<void> {
  await supabase.from('profiles').upsert(profile);
}

export async function markOnboardingDone(userId: string): Promise<void> {
  await supabase.from('profiles').update({ onboarding_done: true }).eq('id', userId);
}
```

```ts
// src/services/vehicles.ts
import { supabase } from '../lib/supabase';
import type { Vehicle } from '../types';

export async function getVehicles(userId: string): Promise<Vehicle[]> {
  const { data } = await supabase.from('vehicles').select('*').eq('user_id', userId).order('created_at');
  return data ?? [];
}

export async function createVehicle(vehicle: Omit<Vehicle, 'id' | 'created_at'>): Promise<Vehicle> {
  const { data, error } = await supabase.from('vehicles').insert(vehicle).select().single();
  if (error) throw error;
  return data;
}
```

- [ ] **6.2 useProfile hook**

```ts
// src/hooks/useProfile.ts
import { useEffect, useState } from 'react';
import { supabase } from '../lib/supabase';
import { getProfile } from '../services/profile';
import type { Profile } from '../types';

export function useProfile() {
  const [profile, setProfile] = useState<Profile | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getUser().then(({ data }) => {
      if (data.user) {
        getProfile(data.user.id).then((p) => {
          setProfile(p);
          setLoading(false);
        });
      }
    });
  }, []);

  return { profile, loading, setProfile };
}
```

- [ ] **6.3 Tela onboarding/locale**

```tsx
// app/onboarding/locale.tsx
import { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';
import { Picker } from '@react-native-picker/picker';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import * as Localization from 'expo-localization';
import { supabase } from '../../src/lib/supabase';
import { upsertProfile } from '../../src/services/profile';

const CURRENCIES = [
  { code: 'BRL', label: 'R$ — Real brasileiro', country: 'BR' },
  { code: 'USD', label: '$ — Dólar americano', country: 'US' },
  { code: 'EUR', label: '€ — Euro', country: 'EU' },
  { code: 'ARS', label: '$ — Peso argentino', country: 'AR' },
  { code: 'CLP', label: '$ — Peso chileno', country: 'CL' },
  { code: 'COP', label: '$ — Peso colombiano', country: 'CO' },
  { code: 'MXN', label: '$ — Peso mexicano', country: 'MX' },
];

export default function LocaleScreen() {
  const { t, i18n } = useTranslation();
  const router = useRouter();
  const [currency, setCurrency] = useState('BRL');
  const [distUnit, setDistUnit] = useState<'km' | 'mi'>('km');
  const [volUnit, setVolUnit] = useState<'liters' | 'gallons'>('liters');
  const [lang, setLang] = useState(Localization.getLocales()[0]?.languageCode ?? 'pt');

  const handleNext = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    await upsertProfile({
      id: data.user.id,
      currency_code: currency,
      distance_unit: distUnit,
      volume_unit: volUnit,
      locale: lang === 'pt' ? 'pt-BR' : lang === 'es' ? 'es-419' : 'en-US',
      name: data.user.email ?? '',
    });
    i18n.changeLanguage(lang);
    router.push('/onboarding/vehicle');
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('onboarding.language_title')}</Text>
      <Text style={s.label}>Idioma</Text>
      <Picker selectedValue={lang} onValueChange={setLang}>
        <Picker.Item label="Português" value="pt" />
        <Picker.Item label="English" value="en" />
        <Picker.Item label="Español" value="es" />
      </Picker>
      <Text style={s.label}>{t('onboarding.currency')}</Text>
      <Picker selectedValue={currency} onValueChange={setCurrency}>
        {CURRENCIES.map((c) => <Picker.Item key={c.code} label={c.label} value={c.code} />)}
      </Picker>
      <Text style={s.label}>{t('onboarding.distance_unit')}</Text>
      <Picker selectedValue={distUnit} onValueChange={(v) => setDistUnit(v as 'km' | 'mi')}>
        <Picker.Item label="Quilômetros (km)" value="km" />
        <Picker.Item label="Milhas (mi)" value="mi" />
      </Picker>
      <Button title={t('onboarding.next')} onPress={handleNext} />
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, padding: 24, paddingTop: 60 },
  title: { fontSize: 22, fontWeight: 'bold', marginBottom: 24 },
  label: { fontSize: 14, color: '#666', marginTop: 16, marginBottom: 4 },
});
```

- [ ] **6.4 Tela onboarding/vehicle**

```tsx
// app/onboarding/vehicle.tsx
import { useState } from 'react';
import { View, Text, TextInput, Switch, Button, StyleSheet, ScrollView } from 'react-native';
import { Picker } from '@react-native-picker/picker';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { supabase } from '../../src/lib/supabase';
import { createVehicle } from '../../src/services/vehicles';
import { decimalToCents } from '../../src/utils/currency';
import { displayToMeters } from '../../src/utils/units';
import type { FuelType, OwnershipType } from '../../src/types';

export default function VehicleScreen() {
  const { t } = useTranslation();
  const router = useRouter();
  const [brand, setBrand] = useState('');
  const [model, setModel] = useState('');
  const [year, setYear] = useState('2020');
  const [fuelType, setFuelType] = useState<FuelType>('gasoline');
  const [consumption, setConsumption] = useState('12'); // km/L
  const [ownership, setOwnership] = useState<OwnershipType>('own');
  const [monthlyCost, setMonthlyCost] = useState('0');
  const [insurance, setInsurance] = useState('0');
  const [isTaxi, setIsTaxi] = useState(false);
  const [taxiLicense, setTaxiLicense] = useState('0');
  const [odometer, setOdometer] = useState('0');

  const handleSave = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user || !brand || !model) return;
    // consumption input: km/L → convert to ml/100km
    const kmPerLiter = parseFloat(consumption);
    const mlPer100km = Math.round((100 / kmPerLiter) * 1000);
    await createVehicle({
      user_id: data.user.id,
      name: `${brand} ${model}`,
      brand,
      model,
      year: parseInt(year),
      fuel_type: fuelType,
      avg_consumption_per_100: mlPer100km,
      ownership_type: ownership,
      monthly_cost_cents: decimalToCents(parseFloat(monthlyCost)),
      monthly_insurance_cents: decimalToCents(parseFloat(insurance)),
      current_odometer: displayToMeters(parseFloat(odometer), 'km'),
      is_taxi: isTaxi,
      taxi_license_monthly_cents: isTaxi ? decimalToCents(parseFloat(taxiLicense)) : 0,
    });
    router.push('/onboarding/goal');
  };

  return (
    <ScrollView contentContainerStyle={s.container}>
      <Text style={s.title}>{t('onboarding.vehicle_title')}</Text>
      <Text style={s.label}>{t('onboarding.brand')}</Text>
      <TextInput style={s.input} value={brand} onChangeText={setBrand} />
      <Text style={s.label}>{t('onboarding.model')}</Text>
      <TextInput style={s.input} value={model} onChangeText={setModel} />
      <Text style={s.label}>{t('onboarding.year')}</Text>
      <TextInput style={s.input} value={year} onChangeText={setYear} keyboardType="numeric" />
      <Text style={s.label}>{t('onboarding.fuel_type')}</Text>
      <Picker selectedValue={fuelType} onValueChange={(v) => setFuelType(v as FuelType)}>
        <Picker.Item label="Gasolina" value="gasoline" />
        <Picker.Item label="Etanol" value="ethanol" />
        <Picker.Item label="Diesel" value="diesel" />
        <Picker.Item label="GNV" value="gnv" />
        <Picker.Item label="Elétrico" value="electric" />
        <Picker.Item label="Híbrido" value="hybrid" />
      </Picker>
      <Text style={s.label}>Consumo (km/L ou mpg)</Text>
      <TextInput style={s.input} value={consumption} onChangeText={setConsumption} keyboardType="decimal-pad" />
      <Text style={s.label}>{t('onboarding.ownership')}</Text>
      <Picker selectedValue={ownership} onValueChange={(v) => setOwnership(v as OwnershipType)}>
        <Picker.Item label="Próprio" value="own" />
        <Picker.Item label="Alugado" value="rent" />
        <Picker.Item label="Financiado" value="financed" />
      </Picker>
      <Text style={s.label}>{t('onboarding.monthly_cost')}</Text>
      <TextInput style={s.input} value={monthlyCost} onChangeText={setMonthlyCost} keyboardType="decimal-pad" />
      <Text style={s.label}>{t('onboarding.insurance')}</Text>
      <TextInput style={s.input} value={insurance} onChangeText={setInsurance} keyboardType="decimal-pad" />
      <Text style={s.label}>Km atual (odômetro)</Text>
      <TextInput style={s.input} value={odometer} onChangeText={setOdometer} keyboardType="numeric" />
      <View style={s.row}>
        <Text style={s.label}>{t('onboarding.is_taxi')}</Text>
        <Switch value={isTaxi} onValueChange={setIsTaxi} />
      </View>
      {isTaxi && (
        <>
          <Text style={s.label}>{t('onboarding.taxi_license')}</Text>
          <TextInput style={s.input} value={taxiLicense} onChangeText={setTaxiLicense} keyboardType="decimal-pad" />
        </>
      )}
      <Button title={t('onboarding.next')} onPress={handleSave} />
    </ScrollView>
  );
}

const s = StyleSheet.create({
  container: { padding: 24, paddingTop: 60 },
  title: { fontSize: 22, fontWeight: 'bold', marginBottom: 24 },
  label: { fontSize: 14, color: '#666', marginTop: 16, marginBottom: 4 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
  row: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: 16 },
});
```

- [ ] **6.5 Tela onboarding/goal**

```tsx
// app/onboarding/goal.tsx
import { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet } from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { supabase } from '../../src/lib/supabase';
import { markOnboardingDone } from '../../src/services/profile';
import { decimalToCents } from '../../src/utils/currency';

export default function GoalScreen() {
  const { t } = useTranslation();
  const router = useRouter();
  const [goal, setGoal] = useState('');

  const handleFinish = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    if (goal) {
      await supabase.from('goals').insert({
        user_id: data.user.id,
        type: 'monthly',
        target_amount_cents: decimalToCents(parseFloat(goal)),
        starts_at: new Date().toISOString().split('T')[0],
      });
    }
    await markOnboardingDone(data.user.id);
    router.replace('/(tabs)');
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('onboarding.goal_title')}</Text>
      <TextInput
        style={s.input}
        placeholder={t('onboarding.goal_placeholder')}
        value={goal}
        onChangeText={setGoal}
        keyboardType="decimal-pad"
      />
      <Button title={t('onboarding.finish')} onPress={handleFinish} />
      <Button title={t('onboarding.skip')} onPress={handleFinish} />
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24 },
  title: { fontSize: 22, fontWeight: 'bold', marginBottom: 24 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12, marginBottom: 16 },
});
```

- [ ] **6.6 Commit**

```bash
git add app/onboarding/ src/services/profile.ts src/services/vehicles.ts src/hooks/useProfile.ts
git commit -m "feat: onboarding — locale, veículo e meta inicial"
```

---

### Task 7: Turnos — Iniciar / Encerrar

**Files:**
- Create: `src/hooks/useActiveShift.ts`
- Create: `src/services/shifts.ts`
- Modify: `app/(tabs)/shifts.tsx`

- [ ] **7.1 Shift service**

```ts
// src/services/shifts.ts
import { supabase } from '../lib/supabase';
import type { Shift } from '../types';

export async function getActiveShift(userId: string): Promise<Shift | null> {
  const { data } = await supabase
    .from('shifts')
    .select('*, shift_earnings(*)')
    .eq('user_id', userId)
    .is('ended_at', null)
    .order('started_at', { ascending: false })
    .limit(1)
    .single();
  return data;
}

export async function startShift(params: {
  user_id: string;
  vehicle_id: string;
  start_odometer: number;  // meters
}): Promise<Shift> {
  const { data, error } = await supabase
    .from('shifts')
    .insert({ ...params, started_at: new Date().toISOString() })
    .select()
    .single();
  if (error) throw error;
  return data;
}

export async function endShift(params: {
  shift_id: string;
  end_odometer: number;  // meters
  earnings: { platform_id: string; gross_amount_cents: number }[];
  tolls_cents: number;
  parking_cents: number;
  food_cents: number;
  tips_cents: number;
  bonuses_cents: number;
}): Promise<void> {
  const { shift_id, end_odometer, earnings, ...costs } = params;

  await supabase
    .from('shifts')
    .update({ ended_at: new Date().toISOString(), end_odometer, ...costs })
    .eq('id', shift_id);

  if (earnings.length > 0) {
    await supabase.from('shift_earnings').insert(
      earnings.map((e) => ({ ...e, shift_id }))
    );
  }

  // trigger edge function
  await supabase.functions.invoke('calculate-shift', { body: { shift_id } });
}

export async function getShifts(userId: string, limit = 30): Promise<Shift[]> {
  const { data } = await supabase
    .from('shifts')
    .select('*, shift_earnings(*)')
    .eq('user_id', userId)
    .not('ended_at', 'is', null)
    .order('started_at', { ascending: false })
    .limit(limit);
  return data ?? [];
}

export async function getTodayShifts(userId: string): Promise<Shift[]> {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const { data } = await supabase
    .from('shifts')
    .select('*, shift_earnings(*)')
    .eq('user_id', userId)
    .gte('started_at', today.toISOString())
    .not('ended_at', 'is', null);
  return data ?? [];
}
```

- [ ] **7.2 useActiveShift hook**

```ts
// src/hooks/useActiveShift.ts
import { useEffect, useState, useCallback } from 'react';
import { supabase } from '../lib/supabase';
import { getActiveShift } from '../services/shifts';
import type { Shift } from '../types';

export function useActiveShift() {
  const [activeShift, setActiveShift] = useState<Shift | null>(null);
  const [loading, setLoading] = useState(true);
  const [elapsedSeconds, setElapsedSeconds] = useState(0);

  const refresh = useCallback(async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    const shift = await getActiveShift(data.user.id);
    setActiveShift(shift);
    setLoading(false);
  }, []);

  useEffect(() => { refresh(); }, [refresh]);

  useEffect(() => {
    if (!activeShift) return;
    const start = new Date(activeShift.started_at).getTime();
    const tick = setInterval(() => {
      setElapsedSeconds(Math.floor((Date.now() - start) / 1000));
    }, 1000);
    return () => clearInterval(tick);
  }, [activeShift]);

  return { activeShift, loading, elapsedSeconds, refresh };
}
```

- [ ] **7.3 Tela de turnos (lista + iniciar/encerrar)**

```tsx
// app/(tabs)/shifts.tsx
import { useState, useEffect } from 'react';
import { View, Text, FlatList, Button, TextInput, StyleSheet, Modal, Alert } from 'react-native';
import { useTranslation } from 'react-i18next';
import { useActiveShift } from '../../src/hooks/useActiveShift';
import { useProfile } from '../../src/hooks/useProfile';
import { startShift, endShift, getShifts } from '../../src/services/shifts';
import { getVehicles } from '../../src/services/vehicles';
import { formatMoney, decimalToCents } from '../../src/utils/currency';
import { displayToMeters, metersToDisplay } from '../../src/utils/units';
import { supabase } from '../../src/lib/supabase';
import type { Shift, Vehicle, Platform } from '../../src/types';

export default function ShiftsScreen() {
  const { t } = useTranslation();
  const { profile } = useProfile();
  const { activeShift, loading, elapsedSeconds, refresh } = useActiveShift();
  const [shifts, setShifts] = useState<Shift[]>([]);
  const [vehicle, setVehicle] = useState<Vehicle | null>(null);
  const [platforms, setPlatforms] = useState<Platform[]>([]);
  const [showStartModal, setShowStartModal] = useState(false);
  const [showEndModal, setShowEndModal] = useState(false);
  const [startOdometer, setStartOdometer] = useState('');
  const [endOdometer, setEndOdometer] = useState('');
  const [earnings, setEarnings] = useState<{ platform_id: string; gross_amount_cents: number; name: string }[]>([]);
  const [tolls, setTolls] = useState('0');
  const [parking, setParking] = useState('0');
  const [food, setFood] = useState('0');
  const [tips, setTips] = useState('0');
  const [bonuses, setBonuses] = useState('0');

  useEffect(() => {
    loadData();
  }, []);

  const loadData = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    const [v, p, s] = await Promise.all([
      getVehicles(data.user.id),
      supabase.from('platforms').select('*').eq('active', true).then(r => r.data ?? []),
      getShifts(data.user.id),
    ]);
    setVehicle(v[0] ?? null);
    setPlatforms(p);
    setShifts(s);
  };

  const handleStart = async () => {
    if (!vehicle || !startOdometer) return;
    await startShift({
      user_id: (await supabase.auth.getUser()).data.user!.id,
      vehicle_id: vehicle.id,
      start_odometer: displayToMeters(parseFloat(startOdometer), profile?.distance_unit ?? 'km'),
    });
    setShowStartModal(false);
    refresh();
  };

  const handleEnd = async () => {
    if (!activeShift || !endOdometer) return;
    await endShift({
      shift_id: activeShift.id,
      end_odometer: displayToMeters(parseFloat(endOdometer), profile?.distance_unit ?? 'km'),
      earnings: earnings.map(e => ({ platform_id: e.platform_id, gross_amount_cents: e.gross_amount_cents })),
      tolls_cents: decimalToCents(parseFloat(tolls)),
      parking_cents: decimalToCents(parseFloat(parking)),
      food_cents: decimalToCents(parseFloat(food)),
      tips_cents: decimalToCents(parseFloat(tips)),
      bonuses_cents: decimalToCents(parseFloat(bonuses)),
    });
    setShowEndModal(false);
    refresh();
    loadData();
  };

  const formatElapsed = (secs: number) => {
    const h = Math.floor(secs / 3600);
    const m = Math.floor((secs % 3600) / 60);
    return `${h}h${m.toString().padStart(2, '0')}m`;
  };

  return (
    <View style={s.container}>
      {activeShift ? (
        <View style={s.activeCard}>
          <Text style={s.activeTitle}>{t('dashboard.active_shift')}</Text>
          <Text style={s.timer}>{formatElapsed(elapsedSeconds)}</Text>
          <Button title={t('shift.end')} onPress={() => setShowEndModal(true)} color="#e74c3c" />
        </View>
      ) : (
        <Button title={t('shift.start')} onPress={() => setShowStartModal(true)} />
      )}

      <FlatList
        data={shifts}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <View style={s.shiftCard}>
            <Text style={s.shiftDate}>
              {new Date(item.started_at).toLocaleDateString(profile?.locale)}
            </Text>
            <Text style={s.shiftNet}>
              {item.calc ? formatMoney(item.calc.net_cents, profile?.currency_code ?? 'BRL', profile?.locale ?? 'pt-BR') : '—'}
            </Text>
            <Text style={s.shiftSub}>
              {item.calc
                ? `${item.calc.duration_hours.toFixed(1)}h · ${metersToDisplay(item.calc.distance_meters, profile?.distance_unit ?? 'km').toFixed(0)} ${profile?.distance_unit ?? 'km'}`
                : ''}
            </Text>
          </View>
        )}
      />

      {/* Modal Iniciar Turno */}
      <Modal visible={showStartModal} animationType="slide" transparent>
        <View style={s.modal}>
          <Text style={s.modalTitle}>{t('shift.start')}</Text>
          <Text style={s.label}>{t('shift.odometer_start')} ({profile?.distance_unit ?? 'km'})</Text>
          <TextInput style={s.input} value={startOdometer} onChangeText={setStartOdometer} keyboardType="numeric" />
          <Button title={t('shift.start')} onPress={handleStart} />
          <Button title={t('common.cancel')} onPress={() => setShowStartModal(false)} />
        </View>
      </Modal>

      {/* Modal Encerrar Turno */}
      <Modal visible={showEndModal} animationType="slide" transparent>
        <View style={s.modal}>
          <Text style={s.modalTitle}>{t('shift.end')}</Text>
          <Text style={s.label}>{t('shift.odometer_end')} ({profile?.distance_unit ?? 'km'})</Text>
          <TextInput style={s.input} value={endOdometer} onChangeText={setEndOdometer} keyboardType="numeric" />
          <Text style={s.sectionTitle}>{t('shift.earnings')}</Text>
          {platforms.slice(0, 5).map(p => (
            <View key={p.id} style={s.earningRow}>
              <Text style={s.platformName}>{p.name}</Text>
              <TextInput
                style={[s.input, { flex: 1 }]}
                placeholder="0"
                keyboardType="decimal-pad"
                onChangeText={(v) => {
                  const cents = decimalToCents(parseFloat(v) || 0);
                  setEarnings(prev => {
                    const existing = prev.findIndex(e => e.platform_id === p.id);
                    if (cents === 0) return prev.filter(e => e.platform_id !== p.id);
                    if (existing >= 0) {
                      const updated = [...prev];
                      updated[existing] = { platform_id: p.id, gross_amount_cents: cents, name: p.name };
                      return updated;
                    }
                    return [...prev, { platform_id: p.id, gross_amount_cents: cents, name: p.name }];
                  });
                }}
              />
            </View>
          ))}
          <Text style={s.sectionTitle}>{t('shift.costs')}</Text>
          {[
            { label: t('shift.tolls'), state: tolls, set: setTolls },
            { label: t('shift.parking'), state: parking, set: setParking },
            { label: t('shift.food'), state: food, set: setFood },
            { label: t('shift.tips'), state: tips, set: setTips },
            { label: t('shift.bonuses'), state: bonuses, set: setBonuses },
          ].map(({ label, state, set }) => (
            <View key={label} style={s.row}>
              <Text style={s.costLabel}>{label}</Text>
              <TextInput style={[s.input, { width: 100 }]} value={state} onChangeText={set} keyboardType="decimal-pad" />
            </View>
          ))}
          <Button title={t('shift.save')} onPress={handleEnd} />
          <Button title={t('common.cancel')} onPress={() => setShowEndModal(false)} />
        </View>
      </Modal>
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, padding: 16, paddingTop: 60 },
  activeCard: { backgroundColor: '#fff3cd', padding: 16, borderRadius: 12, marginBottom: 16, alignItems: 'center' },
  activeTitle: { fontSize: 14, color: '#856404' },
  timer: { fontSize: 36, fontWeight: 'bold', color: '#856404', marginVertical: 8 },
  shiftCard: { backgroundColor: '#fff', padding: 16, borderRadius: 12, marginBottom: 8, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' },
  shiftDate: { color: '#666', fontSize: 13 },
  shiftNet: { fontSize: 18, fontWeight: 'bold' },
  shiftSub: { fontSize: 12, color: '#999' },
  modal: { flex: 1, backgroundColor: '#fff', padding: 24, paddingTop: 60 },
  modalTitle: { fontSize: 22, fontWeight: 'bold', marginBottom: 24 },
  label: { fontSize: 14, color: '#666', marginBottom: 4, marginTop: 12 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
  sectionTitle: { fontSize: 16, fontWeight: '600', marginTop: 20, marginBottom: 8 },
  earningRow: { flexDirection: 'row', alignItems: 'center', gap: 8, marginBottom: 8 },
  platformName: { width: 80, fontSize: 13 },
  row: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 },
  costLabel: { flex: 1, fontSize: 14 },
});
```

- [ ] **7.4 Testar: iniciar turno → timer aparece → encerrar → calc salvo no banco**

```bash
npx expo start
# Verificar no Supabase Studio: shifts.calc tem net_cents, duration_hours
```

- [ ] **7.5 Commit**

```bash
git add app/(tabs)/shifts.tsx src/services/shifts.ts src/hooks/useActiveShift.ts
git commit -m "feat: turnos — iniciar, encerrar com timer e cálculo automático"
```

---

### Task 8: Dashboard

**Files:**
- Modify: `app/(tabs)/index.tsx`

- [ ] **8.1 Dashboard screen**

```tsx
// app/(tabs)/index.tsx
import { useEffect, useState } from 'react';
import { View, Text, ScrollView, StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';
import { useProfile } from '../../src/hooks/useProfile';
import { useActiveShift } from '../../src/hooks/useActiveShift';
import { getTodayShifts } from '../../src/services/shifts';
import { formatMoney } from '../../src/utils/currency';
import { metersToDisplay } from '../../src/utils/units';
import { supabase } from '../../src/lib/supabase';
import type { Shift, Goal } from '../../src/types';

interface DaySummary {
  grossCents: number;
  netCents: number;
  durationHours: number;
  distanceMeters: number;
  netPerHourCents: number;
  netPerMeterCents: number;
  bestPlatform: string;
}

function computeSummary(shifts: Shift[]): DaySummary {
  let grossCents = 0, netCents = 0, durationHours = 0, distanceMeters = 0;
  const platformTotals: Record<string, { name: string; cents: number }> = {};

  for (const s of shifts) {
    if (!s.calc) continue;
    grossCents += s.calc.gross_cents;
    netCents += s.calc.net_cents;
    durationHours += s.calc.duration_hours;
    distanceMeters += s.calc.distance_meters;
    for (const e of s.shift_earnings ?? []) {
      const name = e.platform?.name ?? e.platform_id;
      platformTotals[e.platform_id] = {
        name,
        cents: (platformTotals[e.platform_id]?.cents ?? 0) + e.gross_amount_cents,
      };
    }
  }

  const bestPlatform = Object.values(platformTotals)
    .sort((a, b) => b.cents - a.cents)[0]?.name ?? '—';

  return {
    grossCents, netCents, durationHours, distanceMeters,
    netPerHourCents: durationHours > 0 ? netCents / durationHours : 0,
    netPerMeterCents: distanceMeters > 0 ? netCents / distanceMeters : 0,
    bestPlatform,
  };
}

export default function DashboardScreen() {
  const { t } = useTranslation();
  const { profile } = useProfile();
  const { activeShift, elapsedSeconds } = useActiveShift();
  const [summary, setSummary] = useState<DaySummary | null>(null);
  const [goal, setGoal] = useState<Goal | null>(null);

  const curr = profile?.currency_code ?? 'BRL';
  const locale = profile?.locale ?? 'pt-BR';
  const distUnit = profile?.distance_unit ?? 'km';

  useEffect(() => {
    (async () => {
      const { data } = await supabase.auth.getUser();
      if (!data.user) return;
      const [shifts, goals] = await Promise.all([
        getTodayShifts(data.user.id),
        supabase.from('goals').select('*').eq('user_id', data.user.id).eq('type', 'daily').limit(1).then(r => r.data ?? []),
      ]);
      setSummary(computeSummary(shifts));
      setGoal(goals[0] ?? null);
    })();
  }, [activeShift]);

  const goalProgress = goal && summary
    ? Math.min(1, summary.netCents / goal.target_amount_cents)
    : null;

  const formatElapsed = (s: number) => `${Math.floor(s / 3600)}h${Math.floor((s % 3600) / 60).toString().padStart(2, '0')}m`;

  return (
    <ScrollView style={s.container} contentContainerStyle={s.content}>
      <Text style={s.header}>{t('dashboard.today')}</Text>

      {activeShift && (
        <View style={s.activeCard}>
          <Text style={s.activeLabel}>{t('dashboard.active_shift')}</Text>
          <Text style={s.timer}>{formatElapsed(elapsedSeconds)}</Text>
        </View>
      )}

      {summary ? (
        <>
          <View style={s.row2}>
            <View style={s.card}>
              <Text style={s.cardLabel}>{t('dashboard.gross')}</Text>
              <Text style={s.cardValue}>{formatMoney(summary.grossCents, curr, locale)}</Text>
            </View>
            <View style={[s.card, s.highlight]}>
              <Text style={s.cardLabel}>{t('dashboard.net')}</Text>
              <Text style={s.cardValueLarge}>{formatMoney(summary.netCents, curr, locale)}</Text>
            </View>
          </View>

          <View style={s.row2}>
            <View style={s.card}>
              <Text style={s.cardLabel}>{t('dashboard.net_per_hour')}</Text>
              <Text style={s.cardValue}>{formatMoney(summary.netPerHourCents, curr, locale)}</Text>
            </View>
            <View style={s.card}>
              <Text style={s.cardLabel}>{t('dashboard.net_per_km')}</Text>
              <Text style={s.cardValue}>{formatMoney(summary.netPerMeterCents * 1000, curr, locale)}</Text>
            </View>
          </View>

          <View style={s.infoRow}>
            <Text style={s.info}>
              {summary.durationHours.toFixed(1)} {t('dashboard.hours')} · {metersToDisplay(summary.distanceMeters, distUnit).toFixed(0)} {distUnit}
            </Text>
            <Text style={s.info}>{t('dashboard.best_platform')}: {summary.bestPlatform}</Text>
          </View>

          {goalProgress !== null && (
            <View style={s.goalCard}>
              <Text style={s.goalLabel}>{t('dashboard.goal_progress')}</Text>
              <View style={s.progressBg}>
                <View style={[s.progressFill, { width: `${goalProgress * 100}%` }]} />
              </View>
              <Text style={s.goalPct}>{Math.round(goalProgress * 100)}%</Text>
            </View>
          )}
        </>
      ) : (
        <Text style={s.empty}>{t('dashboard.no_shifts')}</Text>
      )}
    </ScrollView>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5' },
  content: { padding: 16, paddingTop: 60 },
  header: { fontSize: 28, fontWeight: 'bold', marginBottom: 16 },
  activeCard: { backgroundColor: '#fff3cd', padding: 16, borderRadius: 12, marginBottom: 16, alignItems: 'center' },
  activeLabel: { fontSize: 12, color: '#856404' },
  timer: { fontSize: 32, fontWeight: 'bold', color: '#856404' },
  row2: { flexDirection: 'row', gap: 12, marginBottom: 12 },
  card: { flex: 1, backgroundColor: '#fff', padding: 16, borderRadius: 12 },
  highlight: { backgroundColor: '#e8f5e9' },
  cardLabel: { fontSize: 12, color: '#666', marginBottom: 4 },
  cardValue: { fontSize: 18, fontWeight: '600' },
  cardValueLarge: { fontSize: 22, fontWeight: 'bold', color: '#2e7d32' },
  infoRow: { backgroundColor: '#fff', padding: 16, borderRadius: 12, marginBottom: 12 },
  info: { fontSize: 14, color: '#666', marginBottom: 4 },
  goalCard: { backgroundColor: '#fff', padding: 16, borderRadius: 12 },
  goalLabel: { fontSize: 13, color: '#666', marginBottom: 8 },
  progressBg: { height: 8, backgroundColor: '#e0e0e0', borderRadius: 4 },
  progressFill: { height: 8, backgroundColor: '#2e7d32', borderRadius: 4 },
  goalPct: { fontSize: 13, color: '#2e7d32', marginTop: 4, textAlign: 'right' },
  empty: { textAlign: 'center', color: '#999', marginTop: 40 },
});
```

- [ ] **8.2 Commit**

```bash
git add app/(tabs)/index.tsx
git commit -m "feat: dashboard — lucro líquido, horas, km, melhor plataforma, meta"
```

---

### Task 9: Combustível

**Files:**
- Create: `src/services/fuel.ts`
- Modify: `app/(tabs)/fuel.tsx`

- [ ] **9.1 Fuel service**

```ts
// src/services/fuel.ts
import { supabase } from '../lib/supabase';
import type { FuelEntry } from '../types';

export async function getFuelEntries(userId: string, limit = 20): Promise<FuelEntry[]> {
  const { data } = await supabase
    .from('fuel_entries')
    .select('*')
    .eq('user_id', userId)
    .order('filled_at', { ascending: false })
    .limit(limit);
  return data ?? [];
}

export async function addFuelEntry(entry: Omit<FuelEntry, 'id'>): Promise<void> {
  const { error } = await supabase.from('fuel_entries').insert(entry);
  if (error) throw error;
}

export async function getLastFuelPrice(vehicleId: string): Promise<number> {
  const { data } = await supabase
    .from('fuel_entries')
    .select('price_per_unit_cents')
    .eq('vehicle_id', vehicleId)
    .order('filled_at', { ascending: false })
    .limit(1)
    .single();
  return data?.price_per_unit_cents ?? 0;
}
```

- [ ] **9.2 Tela de combustível**

```tsx
// app/(tabs)/fuel.tsx
import { useState, useEffect } from 'react';
import { View, Text, FlatList, TextInput, Button, Modal, StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';
import { useProfile } from '../../src/hooks/useProfile';
import { getFuelEntries, addFuelEntry } from '../../src/services/fuel';
import { getVehicles } from '../../src/services/vehicles';
import { formatMoney, decimalToCents } from '../../src/utils/currency';
import { displayToMeters, mlToDisplay, displayToMl } from '../../src/utils/units';
import { supabase } from '../../src/lib/supabase';
import type { FuelEntry, Vehicle } from '../../src/types';

export default function FuelScreen() {
  const { t } = useTranslation();
  const { profile } = useProfile();
  const [entries, setEntries] = useState<FuelEntry[]>([]);
  const [vehicle, setVehicle] = useState<Vehicle | null>(null);
  const [showModal, setShowModal] = useState(false);
  const [odometer, setOdometer] = useState('');
  const [volume, setVolume] = useState('');
  const [total, setTotal] = useState('');
  const [station, setStation] = useState('');

  const curr = profile?.currency_code ?? 'BRL';
  const locale = profile?.locale ?? 'pt-BR';
  const volUnit = profile?.volume_unit ?? 'liters';

  useEffect(() => { loadData(); }, []);

  const loadData = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    const [v, e] = await Promise.all([
      getVehicles(data.user.id),
      getFuelEntries(data.user.id),
    ]);
    setVehicle(v[0] ?? null);
    setEntries(e);
  };

  const handleSave = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user || !vehicle) return;
    const vol_ml = displayToMl(parseFloat(volume), volUnit);
    const total_cents = decimalToCents(parseFloat(total));
    const price_per_unit = Math.round(total_cents / (parseFloat(volume) || 1));
    await addFuelEntry({
      user_id: data.user.id,
      vehicle_id: vehicle.id,
      filled_at: new Date().toISOString(),
      odometer: displayToMeters(parseFloat(odometer), profile?.distance_unit ?? 'km'),
      fuel_type: vehicle.fuel_type,
      volume_ml: vol_ml,
      total_amount_cents: total_cents,
      price_per_unit_cents: price_per_unit,
      station_name: station || undefined,
      is_full_tank: true,
    });
    setShowModal(false);
    loadData();
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('fuel.title')}</Text>
      <Button title={t('fuel.add')} onPress={() => setShowModal(true)} />
      <FlatList
        data={entries}
        keyExtractor={(e) => e.id}
        renderItem={({ item }) => (
          <View style={s.card}>
            <Text style={s.date}>{new Date(item.filled_at).toLocaleDateString(locale)}</Text>
            <Text style={s.amount}>{formatMoney(item.total_amount_cents, curr, locale)}</Text>
            <Text style={s.sub}>
              {mlToDisplay(item.volume_ml, volUnit).toFixed(1)} {volUnit === 'liters' ? 'L' : 'gal'} · {formatMoney(item.price_per_unit_cents, curr, locale)}/{volUnit === 'liters' ? 'L' : 'gal'}
            </Text>
          </View>
        )}
      />
      <Modal visible={showModal} animationType="slide">
        <View style={s.modal}>
          <Text style={s.title}>{t('fuel.add')}</Text>
          {[
            { label: `${t('fuel.odometer')} (${profile?.distance_unit ?? 'km'})`, val: odometer, set: setOdometer },
            { label: `${t('fuel.liters')} (${volUnit === 'liters' ? 'L' : 'gal'})`, val: volume, set: setVolume },
            { label: t('fuel.total'), val: total, set: setTotal },
            { label: t('fuel.station'), val: station, set: setStation },
          ].map(({ label, val, set }) => (
            <View key={label}>
              <Text style={s.label}>{label}</Text>
              <TextInput style={s.input} value={val} onChangeText={set} keyboardType={label.includes('Posto') ? 'default' : 'decimal-pad'} />
            </View>
          ))}
          <Button title={t('fuel.save')} onPress={handleSave} />
          <Button title={t('common.cancel')} onPress={() => setShowModal(false)} />
        </View>
      </Modal>
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, padding: 16, paddingTop: 60 },
  title: { fontSize: 22, fontWeight: 'bold', marginBottom: 16 },
  card: { backgroundColor: '#fff', padding: 16, borderRadius: 12, marginBottom: 8 },
  date: { fontSize: 13, color: '#666' },
  amount: { fontSize: 18, fontWeight: 'bold', marginVertical: 4 },
  sub: { fontSize: 13, color: '#999' },
  modal: { flex: 1, padding: 24, paddingTop: 60 },
  label: { fontSize: 14, color: '#666', marginTop: 12, marginBottom: 4 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
});
```

- [ ] **9.3 Commit**

```bash
git add app/(tabs)/fuel.tsx src/services/fuel.ts
git commit -m "feat: módulo de combustível — registro e histórico"
```

---

### Task 10: Despesas

**Files:**
- Create: `src/services/expenses.ts`
- Modify: `app/(tabs)/expenses.tsx`

- [ ] **10.1 Expense service**

```ts
// src/services/expenses.ts
import { supabase } from '../lib/supabase';
import type { Expense, ExpenseCategory } from '../types';

export async function getExpenseCategories(): Promise<ExpenseCategory[]> {
  const { data } = await supabase.from('expense_categories').select('*').order('type').order('name_key');
  return data ?? [];
}

export async function getExpenses(userId: string, limit = 30): Promise<Expense[]> {
  const { data } = await supabase
    .from('expenses')
    .select('*, category:expense_categories(*)')
    .eq('user_id', userId)
    .order('expense_date', { ascending: false })
    .limit(limit);
  return data ?? [];
}

export async function addExpense(expense: Omit<Expense, 'id' | 'category'>): Promise<void> {
  const { error } = await supabase.from('expenses').insert(expense);
  if (error) throw error;
}
```

- [ ] **10.2 Tela de despesas**

```tsx
// app/(tabs)/expenses.tsx
import { useState, useEffect } from 'react';
import { View, Text, FlatList, Button, Modal, TextInput, StyleSheet } from 'react-native';
import { Picker } from '@react-native-picker/picker';
import { useTranslation } from 'react-i18next';
import { useProfile } from '../../src/hooks/useProfile';
import { getExpenses, getExpenseCategories, addExpense } from '../../src/services/expenses';
import { formatMoney, decimalToCents } from '../../src/utils/currency';
import { supabase } from '../../src/lib/supabase';
import type { Expense, ExpenseCategory } from '../../src/types';

export default function ExpensesScreen() {
  const { t } = useTranslation();
  const { profile } = useProfile();
  const [expenses, setExpenses] = useState<Expense[]>([]);
  const [categories, setCategories] = useState<ExpenseCategory[]>([]);
  const [showModal, setShowModal] = useState(false);
  const [categoryId, setCategoryId] = useState('');
  const [amount, setAmount] = useState('');
  const [description, setDescription] = useState('');

  const curr = profile?.currency_code ?? 'BRL';
  const locale = profile?.locale ?? 'pt-BR';

  useEffect(() => { loadData(); }, []);

  const loadData = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user) return;
    const [e, c] = await Promise.all([getExpenses(data.user.id), getExpenseCategories()]);
    setExpenses(e);
    setCategories(c);
    if (c.length > 0) setCategoryId(c[0].id);
  };

  const handleSave = async () => {
    const { data } = await supabase.auth.getUser();
    if (!data.user || !categoryId || !amount) return;
    await addExpense({
      user_id: data.user.id,
      category_id: categoryId,
      expense_date: new Date().toISOString().split('T')[0],
      amount_cents: decimalToCents(parseFloat(amount)),
      description: description || undefined,
      is_recurring: false,
    });
    setShowModal(false);
    loadData();
  };

  return (
    <View style={s.container}>
      <Text style={s.title}>{t('expense.title')}</Text>
      <Button title={t('expense.add')} onPress={() => setShowModal(true)} />
      <FlatList
        data={expenses}
        keyExtractor={(e) => e.id}
        renderItem={({ item }) => (
          <View style={s.card}>
            <Text style={s.date}>{new Date(item.expense_date).toLocaleDateString(locale)}</Text>
            <Text style={s.amount}>{formatMoney(item.amount_cents, curr, locale)}</Text>
            <Text style={s.cat}>{t(item.category?.name_key ?? item.category_id)}</Text>
            {item.description ? <Text style={s.desc}>{item.description}</Text> : null}
          </View>
        )}
      />
      <Modal visible={showModal} animationType="slide">
        <View style={s.modal}>
          <Text style={s.title}>{t('expense.add')}</Text>
          <Text style={s.label}>{t('expense.category')}</Text>
          <Picker selectedValue={categoryId} onValueChange={setCategoryId}>
            {categories.map(c => (
              <Picker.Item key={c.id} label={t(c.name_key)} value={c.id} />
            ))}
          </Picker>
          <Text style={s.label}>{t('expense.amount')}</Text>
          <TextInput style={s.input} value={amount} onChangeText={setAmount} keyboardType="decimal-pad" />
          <Text style={s.label}>{t('expense.description')}</Text>
          <TextInput style={s.input} value={description} onChangeText={setDescription} />
          <Button title={t('expense.save')} onPress={handleSave} />
          <Button title={t('common.cancel')} onPress={() => setShowModal(false)} />
        </View>
      </Modal>
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, padding: 16, paddingTop: 60 },
  title: { fontSize: 22, fontWeight: 'bold', marginBottom: 16 },
  card: { backgroundColor: '#fff', padding: 16, borderRadius: 12, marginBottom: 8 },
  date: { fontSize: 12, color: '#999' },
  amount: { fontSize: 18, fontWeight: 'bold', marginVertical: 4 },
  cat: { fontSize: 13, color: '#666' },
  desc: { fontSize: 13, color: '#999', marginTop: 2 },
  modal: { flex: 1, padding: 24, paddingTop: 60 },
  label: { fontSize: 14, color: '#666', marginTop: 12, marginBottom: 4 },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
});
```

- [ ] **10.3 Commit**

```bash
git add app/(tabs)/expenses.tsx src/services/expenses.ts
git commit -m "feat: módulo de despesas — registro por categoria"
```

---

### Task 11: Tab Navigation + More Screen

**Files:**
- Modify: `app/(tabs)/_layout.tsx`
- Create: `app/(tabs)/more.tsx`

- [ ] **11.1 Tab layout**

```tsx
// app/(tabs)/_layout.tsx
import { Tabs } from 'expo-router';
import { useTranslation } from 'react-i18next';

export default function TabLayout() {
  const { t } = useTranslation();
  return (
    <Tabs screenOptions={{ headerShown: false }}>
      <Tabs.Screen name="index" options={{ title: t('tabs.dashboard') }} />
      <Tabs.Screen name="shifts" options={{ title: t('tabs.shifts') }} />
      <Tabs.Screen name="fuel" options={{ title: t('tabs.fuel') }} />
      <Tabs.Screen name="expenses" options={{ title: t('tabs.expenses') }} />
      <Tabs.Screen name="more" options={{ title: t('tabs.more') }} />
    </Tabs>
  );
}
```

- [ ] **11.2 More screen**

```tsx
// app/(tabs)/more.tsx
import { View, Text, Button, StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';
import { useRouter } from 'expo-router';
import { useAuth } from '../../src/hooks/useAuth';
import { useProfile } from '../../src/hooks/useProfile';
import { formatMoney } from '../../src/utils/currency';

export default function MoreScreen() {
  const { t } = useTranslation();
  const { signOut } = useAuth();
  const { profile } = useProfile();
  const router = useRouter();

  return (
    <View style={s.container}>
      <Text style={s.name}>{profile?.name ?? '—'}</Text>
      <Text style={s.sub}>{profile?.currency_code} · {profile?.distance_unit} · {profile?.locale}</Text>
      <Button title={t('auth.logout')} onPress={signOut} color="#e74c3c" />
    </View>
  );
}

const s = StyleSheet.create({
  container: { flex: 1, padding: 24, paddingTop: 60 },
  name: { fontSize: 22, fontWeight: 'bold', marginBottom: 8 },
  sub: { fontSize: 14, color: '#666', marginBottom: 32 },
});
```

- [ ] **11.3 Testes finais de smoke — fluxo completo**

```bash
npx expo start
# Percorrer:
# 1. Register → onboarding (locale → vehicle → goal) → dashboard
# 2. Shifts → Iniciar → timer aparece no dashboard → Encerrar → calc no card
# 3. Fuel → Registrar abastecimento → aparece na lista
# 4. Expenses → Nova despesa → aparece na lista
# 5. More → Logout → volta para login
```

- [ ] **11.4 Commit final do MVP**

```bash
git add app/(tabs)/
git commit -m "feat: navegação por tabs e tela de configurações

MVP Fase 1 completo:
- Auth (login/register)
- Onboarding (locale, veículo, meta)
- Edge function calculate-shift com testes
- Turnos com timer ao vivo
- Dashboard com lucro líquido, horas, km, melhor plataforma
- Combustível
- Despesas por categoria
- i18n pt/en/es, multi-moeda, multi-unidade"
```

---

## Self-Review

### Spec coverage
| Seção da spec | Task que cobre |
|---|---|
| Arquitetura (Expo Router + Supabase + Edge Functions) | Task 1, 4 |
| Modelo de dados (todas as tabelas) | Task 2 |
| i18n (pt/en/es, moeda, unidade) | Task 3 |
| Auth (email + Google) | Task 5 (Google OAuth: fase 2) |
| Onboarding | Task 6 |
| Turno em duas etapas com timestamps | Task 7 |
| Edge Function calculate-shift com testes | Task 4 |
| Dashboard com turno ativo, lucro, meta | Task 8 |
| Combustível | Task 9 |
| Despesas por categoria | Task 10 |
| Tab navigation | Task 11 |
| Planejamento financeiro (manutenção, troca, cenários) | **Fase 2 — plano separado** |
| Relatórios + export PDF/CSV | **Fase 2 — plano separado** |
| Metas avançadas | **Fase 2 — plano separado** |
| Google OAuth | **Fase 2 — plano separado** |

### Notas
- Todos os valores monetários em centavos no banco ✓
- Todos os cálculos na Edge Function, não no cliente ✓
- RLS em todas as tabelas ✓
- Sem strings hardcoded em PT ✓
- Timestamps em UTC ✓
